<script type="module" src="@Assets["UI/Shared/Layout/ReconnectModal.razor.js"]"></script>

<dialog id="components-reconnect-modal" data-nosnippet 
    style="margin: 0;
    width: 100%;
    max-width: 100%;
    height: 100%;
    max-height: 100%;
    background-color: #333 !important;
    position: absolute;
    left: 0;
    top: 0;">
    <div class="components-reconnect-container" 
        style="margin: auto;
        margin-top: 50px;
        width: 400px;
        background: #fcfcfc;
        padding: 25px;
        border-radius: 10px;">
        <div class="components-rejoining-animation" aria-hidden="true">
            <div></div>
            <div></div>
        </div>
        <p class="components-reconnect-first-attempt-visible">
            Rejoining the server...
        </p>
        <p class="components-reconnect-repeated-attempt-visible">
            Rejoin failed... trying again in <span id="components-seconds-to-next-attempt"></span> seconds.
        </p>
        <p class="components-reconnect-failed-visible">
            Failed to rejoin.<br />Please retry or reload the page.
        </p>
        <button id="components-reconnect-button" class="components-reconnect-failed-visible btn-primary">
            Retry
        </button>
        <p class="components-pause-visible">
            The session has been paused by the server.
        </p>
        <button id="components-resume-button" class="components-pause-visible btn-secondary">
            Resume
        </button>
        <p class="components-resume-failed-visible">
            Failed to resume the session.<br />Please reload the page.
        </p>
    </div>
</dialog>
